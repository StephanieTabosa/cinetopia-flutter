import 'package:flutter/material.dart';

class MoviesDetailsTemplate extends StatelessWidget {
  const MoviesDetailsTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}

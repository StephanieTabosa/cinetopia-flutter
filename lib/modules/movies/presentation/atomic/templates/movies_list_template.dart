import 'package:flutter/material.dart';

class MoviesListTemplate extends StatelessWidget {
  const MoviesListTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
